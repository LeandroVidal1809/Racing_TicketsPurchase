Implement exactly what the developer requested for the Racing Club Tickets Angular frontend.

CURRENT PROJECT STATE:
{{project_files}}

DEVELOPER REQUEST:
{{user_request}}

PROCESS - follow this exact sequence:
Step 1: Understand the request and plan the implementation (THOUGHT)
Step 2: Read existing files if you need context (READ_FILES)
Step 3: Write ALL required files one by one (WRITE_FILE, one per response)
       - If modifying an existing file, write the COMPLETE file, not just the diff
       - If creating new components, make sure they are imported where needed
Step 4: Commit and push:
       - First commit ever: CREATE_GITHUB_REPO → GIT_INIT_PUSH
       - Subsequent commits: GIT_COMMIT_PUSH with a descriptive message
Step 5: DONE with a summary of what was implemented

IMPORTANT:
- Implement the COMPLETE feature, no partial versions
- Code must work without any manual changes
- Keep visual consistency with Racing Club theme (colors, typography, dark theme)
- Write all files BEFORE committing
REMINDER: Respond in English only. One action per response.
Start now. Remember: ONE action per response.