You are an Angular 17 expert. Write the complete content of the following file.

FILE TO WRITE: {{file_path}}
FILE TYPE: {{file_type}}

DEVELOPER REQUEST (what this file must implement):
{{user_request}}

PROJECT CONTEXT - Racing Club de Avellaneda ticket sales website:
- Angular 17+ standalone components
- Dark theme with glassmorphism cards
- Colors: --color-primary #1565C0, --color-accent #00B0FF, --color-danger #D32F2F
- Backgrounds: --bg-base #0D1B2A, --bg-surface #112236, --bg-glass rgba(21,44,68,0.7)
- Text: --text-primary #E8F0FE, --text-secondary #90A4C8
- Fonts (already loaded): --font-display 'Barlow Condensed' (headings weight 700-900), --font-body 'Barlow'
- Numbers/prices font: 'Oswald' weight 600
- CSS variables defined in styles.scss, use them via var(--name)
- Reusable CSS classes: .container, .btn.btn--primary, .btn--outline, .card, .section, .section__title
- Badge classes: .badge.badge--available, .badge--few, .badge--soldout
- Animation class: .animate-slide-up (staggered entrance animation)
- PartidosService methods: getPartidos(), getPartidoById(id), comprarTicket(orden), getMisTickets()
- Models: Partido, Sector, Ticket, OrdenCompra
- Helper functions: getPrecioMinimo(partido), getDisponibilidad(sector), getDisponibilidadLabel(estado)
- All imports from: ../../core/services/partidos.service and ../../core/models/partido.model

EXISTING PROJECT FILES (for import reference):
{{existing_files}}

OUTPUT RULES - critical:
- Output the raw file content ONLY
- Do NOT wrap in markdown code fences (no ```)
- Do NOT add any explanation before or after the code
- Write COMPLETE, production-ready code
- No TODOs, no placeholders, no comments saying "add logic here"
- For .ts files: always use standalone: true, import CommonModule and RouterLink when needed
- For .html files: use *ngIf and *ngFor (not @if/@for), bind only properties that exist in the .ts
- For .scss files: use BEM naming (.block__element--modifier), use CSS variables, include @media (max-width: 768px)

Write the file now: