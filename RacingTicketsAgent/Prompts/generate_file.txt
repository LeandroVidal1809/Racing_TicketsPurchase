You are an Angular 17 expert. Write the complete content of this file for a ticket sales website for Racing Club de Avellaneda.

FILE: {{file_path}}
TYPE: {{file_type}}
REQUEST: {{user_request}}

PROJECT DESIGN SYSTEM:
CSS variables (always use var(), never hardcode hex):
  --color-primary: #1565C0  --color-accent: #00B0FF  --color-danger: #D32F2F
  --bg-base: #0D1B2A  --bg-surface: #112236  --bg-glass: rgba(21,44,68,0.7)
  --text-primary: #E8F0FE  --text-secondary: #90A4C8
  --font-display: 'Barlow Condensed'  --font-body: 'Barlow'
  --space-sm: 8px  --space-md: 16px  --space-lg: 24px  --space-xl: 40px  --space-2xl: 64px
  --radius-sm: 6px  --radius-md: 12px  --shadow-card: 0 4px 24px rgba(0,0,0,0.4)

Reusable CSS classes already defined in styles.scss:
  .container .btn.btn--primary .btn--outline .btn--danger
  .card .section .section__title
  .badge.badge--available .badge--few .badge--soldout
  .animate-slide-up

Available service — PartidosService (import from ../../core/services/partidos.service):
  getPartidos(): Observable<Partido[]>
  getPartidoById(id: string): Observable<Partido | undefined>
  comprarTicket(orden: OrdenCompra): Observable<Ticket>
  getMisTickets(): Observable<Ticket[]>

Available models (import from ../../core/models/partido.model):
  Partido, Sector, Ticket, OrdenCompra
  getPrecioMinimo(partido): number
  getDisponibilidad(sector): 'available' | 'few' | 'soldout'
  getDisponibilidadLabel(estado): string

Existing project files (for import paths):
{{existing_files}}

RULES BY FILE TYPE:

For .ts files:
  - standalone: true
  - imports array must include CommonModule always
  - imports array must include RouterLink if template uses routerLink or routerLinkActive
  - imports array must include CurrencyPipe and/or DatePipe if template uses them
  - Every property used in the template must be declared in the class
  - Every method called from the template must exist in the class
  - Use ngOnInit() to load data from services

For .html files:
  - Use *ngIf and *ngFor (never @if or @for)
  - Every *ngFor must have trackBy
  - Always show a loading state and an empty state
  - Only reference properties and methods that exist in the .ts file
  - Never use inline styles, only CSS classes

For .scss files:
  - BEM methodology: .block__element--modifier
  - Always use var(--variable-name) for colors, fonts and spacing
  - Write mobile styles inside @media (max-width: 768px)
  - Add transition: all 0.25s ease on interactive elements

OUTPUT RULES — critical:
  - Output the raw file content only
  - Do not add any explanation before or after
  - Do not use markdown code fences or backticks
  - The first character of your response must be the first character of the file
